#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    pair<string, int> p;
    
    p.first = "Tashii";
    p.second = 15;

    cout<<p.first<<' '<<p.second<<'\n';

    return 0;
}